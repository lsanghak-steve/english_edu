{
  "name": "english_edu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd next_app && npm run dev",
    "build": "cd next_app && npm install && npm run build",
    "start": "cd next_app && npm run start",
    "postinstall": "cd next_app && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0"
  }
}
