{
  "name": "english_edu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix next_app run dev",
    "build": "npm --prefix next_app run build",
    "start": "npm --prefix next_app run start"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  }
}
